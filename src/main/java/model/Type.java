package model;

enum Type {
    CAT,
    BOOK,
    GAME,
    FRAME,
    TROPHY,
    PLANT,
    NOTHING,  //NO TILES IN THIS MOMENT: we have yet to chose if this will be the definitive choice
    BLOCKED    // it isn't a proper tile, it is just to block a cell
}
